# 2026-03-27

## 完成：修复编译错误 + 优化 GitHub Actions 工作流

### 编译修复
1. `ImageLoader.kt`：`object ImageLoader` → `object AppImageLoader`，消除与 Coil 的命名冲突
2. `BookMappers.kt`：成员扩展函数移出 object 改为文件级顶层函数，修复静态导入问题；同步更新 `BookRepositoryImpl.kt` 的导入路径
3. `ImageUtils.kt`：lint 检查无错，原空安全写法已正确

### 工作流优化
- `ci.yml`：移除弃用的 `android-actions/setup-android@v3`，改用 ubuntu-latest 内置 `$ANDROID_HOME`
- `release.yml`：同上 SDK 修复 + 添加 keystore 解码签名步骤（via `KEYSTORE_BASE64` 等 GitHub Secrets）
- `app/build.gradle.kts`：添加 release 签名配置，通过环境变量注入，本地降级 debug 签名

### 验证结果
- assembleDebug：BUILD SUCCESSFUL（47s）
- assembleRelease：BUILD SUCCESSFUL（3m 24s）
