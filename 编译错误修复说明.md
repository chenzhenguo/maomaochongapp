# 编译错误修复说明

## 编译失败原因

编译 Release 版本时遇到以下错误：

### 1. ImageLoader.kt 错误

```kotlin
// 错误 1: availableMemoryPercentage 已废弃
.availableMemoryPercentage(0.25)
// 修复：使用 memoryCache

// 错误 2: 缺少 BuildConfig 导入
if (BuildConfig.DEBUG)
// 修复：添加 import com.maomaochongapp.BuildConfig

// 错误 3: coil.ImageLoader 应为 ImageLoader
coil.ImageLoader.setGlobal(imageLoader)
// 修复：使用 ImageLoader.setGlobal(imageLoader)
```

### 2. PictureBookViewModel.kt 错误

```kotlin
// 错误 1: 缺少 supervisorScope 导入
// 修复：添加 import kotlinx.coroutines.supervisorScope

// 错误 2: 缺少 CancellationException 导入
// 修复：添加 import kotlinx.coroutines.CancellationException
```

### 3. BookRepositoryImpl.kt 错误

```kotlin
// 错误：缺少 toDomain 和 toEntity 导入
// 修复：添加 import com.maomaochongapp.picturebook.data.mapper.toDomain
//      添加 import com.maomaochongapp.picturebook.data.mapper.toEntity
```

### 4. ImageUtils.kt 错误

```kotlin
// 错误：类型不匹配 Long? -> Long
// 修复：添加 null 检查
```

---

## 修复方法

### 方法 1: 运行修复脚本

```bash
cd D:\ideaworkspace\maomaochongapp
python 修复编译错误.py
```

### 方法 2: 手动修复

#### 修复 ImageLoader.kt

文件位置：`app/src/main/java/com/maomaochongapp/core/image/ImageLoader.kt`

```kotlin
// 添加导入
import android.os.Build
import com.maomaochongapp.BuildConfig

// 修改第 25 行
.availableMemoryPercentage(0.25)  // 删除或注释此行

// 修改第 53 行
coil.ImageLoader.setGlobal(imageLoader)  // 改为 ImageLoader.setGlobal(imageLoader)

// 修改第 59-60 行
coil.ImageLoader.get(context)  // 改为 ImageLoader.get(context)
```

#### 修复 PictureBookViewModel.kt

文件位置：`app/src/main/java/com/maomaochongapp/picturebook/ui/viewmodel/PictureBookViewModel.kt`

```kotlin
// 添加导入
import kotlinx.coroutines.supervisorScope
import kotlinx.coroutines.CancellationException
```

#### 修复 BookRepositoryImpl.kt

文件位置：`app/src/main/java/com/maomaochongapp/picturebook/data/repository/BookRepositoryImpl.kt`

```kotlin
// 添加导入
import com.maomaochongapp.picturebook.data.mapper.toDomain
import com.maomaochongapp.picturebook.data.mapper.toEntity
```

---

## 重新编译

修复完成后，运行以下命令重新编译：

```bash
cd D:\ideaworkspace\maomaochongapp
gradlew clean assembleRelease
```

或使用批处理文件：

```bash
编译发布.bat
```

选择选项 3: 编译 Release 版本

---

## 输出位置

编译成功后，APK 文件位置：

```
app\build\outputs\apk\release\app-release.apk
```

---

## 版本信息

- **版本**: 1.0.0
- **versionCode**: 1
- **minSdk**: 26
- **targetSdk**: 35
- **已混淆**: 是
- **已压缩资源**: 是
